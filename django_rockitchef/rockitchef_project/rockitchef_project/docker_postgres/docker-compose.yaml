version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: recipes
      POSTGRES_USER: rockitmaster
      POSTGRES_PASSWORD: Testingmybiscuits
    ports:
        - "5432:5432"
