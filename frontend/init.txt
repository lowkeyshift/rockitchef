# to set up dev env
# you need to install yarn somehow
# then running yarn init should reference package.json and load you the relevent tools
# right now its
# live-server : so you can access app via localhost:8080
# babel-cli : so you can convert JSX code to js
 yarn init



 -----

 babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch
 # edit around app.js written in ES5/6 and see it reflected in public scripts

 live-server public
 #uses live-server to host something on localhost:8080 using contents of public folder